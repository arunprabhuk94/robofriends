version: 2.1
jobs:
    build:
        docker: 
            - image: circleci/node:4.8.2
        steps:
            - checkout
            - run: echo "hello world"
    build2:
        docker: 
            - image: circleci/node:4.8.2
        steps:
            - checkout
            - run: echo "Double Trouble"
    workflows:
        version: 2
        bobby_sally:
            jobs:
                - build
                - build2